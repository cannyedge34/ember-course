{{link-to 'Back to list of companies' 'orgs'}}

<h4>Facebook</h4>

<span {{action 'addToRecords' 'Two'}}>Add me</span>

<ul>
  {{#each records as |record|}}
    <li>
      {{record.id}}
    </li>
  {{/each}}
</ul>

{{log records}}
{{outlet}}