<h2>Big Orgs</h2>

<ul>
  {{#each model as |org|}}
    <li>
      <span {{ action "favoriteClicked" org }}>
        Favorite
      </span>
      {{link-to org.id "org" org.id }}
    </li>
  {{/each}}
</ul>

{{outlet}}
